---
name: example
description: Starter prompt bundled with the Contoso Copilot plugin.
---

# /example

Run a one-line smoke test of the plugin: report the plugin name, version, and
the entrypoints loaded. Useful as a first command after install to confirm
the plugin is active.
