# Accuracy Ledger — Decisions (2026-04-24)

Auditable output of the WS-B accuracy-ledger sweep. One row per candidate edit listed in plan §"Candidate edits". The three-question gate (plan §174–180) was applied to each; only rows with `decision: apply` received a soft callout. Files in [`docs/_meta/accuracy-ledger-exclusions.md`](_meta/accuracy-ledger-exclusions.md) are excluded from the candidate set entirely (not merely from edits).

| claim | docs URL | files affected (post-exclusion) | Q1 — materially wrong? | Q2 — would attendee fail or learn falsehood? | Q3 — fits ≤ 2 sentences without flow rewrite? | decision |
| --- | --- | --- | --- | --- | --- | --- |
| SKILL.md supports a `model:` frontmatter field | https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-skills | (no candidate files outside exclusion list — `plugin-template/agents/example-agent.md` matched `^model:` but is a custom agent file, not a SKILL.md, where `model:` is documented) | n/a | n/a | n/a | drop — no in-bounds claim of `model:` in any SKILL.md |
| Prompt files (`.prompt.md`) work in Copilot CLI | https://docs.github.com/en/copilot/tutorials/customization-library/prompt-files/your-first-prompt-file | `labs/lab04.md`, `labs/appendices/dotnet/lab04.md`, `labs/appendices/node/lab04.md`, `docs/token-and-model-guide.md` | yes — docs explicitly scope prompt files to VS Code / Visual Studio / JetBrains; CLI exposes no `@prompt` or `/prompt` slash command for user-authored `.prompt.md` files | yes — a CLI-only attendee invoking `@create-dotnet-test` (lab04) or `/cost-check` (token guide) sees nothing happen and concludes the lab is broken | yes — single blockquote pointing at the IDE-only docs page; no surrounding step changes | apply |
| `infer:` is the current way to control auto-delegation for custom agents | https://docs.github.com/en/copilot/reference/custom-agents-configuration | (no candidate files outside exclusion list — only `labs/lab-hardening.md`, `solutions/lab-hardening/tight-reviewer.agent.md`, `docs/copilot-config-reference.md`, all WS-B-authored / sibling-output) | n/a | n/a | n/a | drop — no in-bounds teaching of `infer:` survives after exclusion subtraction |
| Default Copilot CLI model is GPT-5 (or similar fixed name) | https://docs.github.com/en/copilot/concepts/agents/about-copilot-cli | (`labs/lab07.md`, `docs/token-and-model-guide.md`, `workshop/slides/30-module-3.md`, `workshop/slides/60-module-6.md`, `docs/_meta/registry.yaml`, `workshop/speaker-scripts/module-06-a2a-tmux.md` — all in scope, but claim is structural narrative) | partial — claim shifts as defaults change; current docs / README show Claude Sonnet 4.5 as default | unlikely — model picker still works regardless of the named default | no — the claim is woven into capacity/cost narrative across multiple paragraphs in each file; a 2-sentence soft note doesn't fit without rewriting surrounding flow | drop — structural narrative; covered by V.4 audit per claim-reconciliation #11 (`noted-only`), no in-place callout fits without rewriting surrounding text |
| MCP `sse` is current / recommended | https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-mcp-servers | `workshop/slides/20-module-2.md` | yes — the MCP spec deprecated `sse` in favor of Streamable HTTP (`http`); docs accept `sse` only for backward compat | borderline yes — attendee builds a new server with `type: sse` and ships a deprecated transport into prod | yes — single callout naming the deprecation and the preferred type | apply |
| `preToolUse` hooks support `allow` / `ask` decisions | https://docs.github.com/en/copilot/reference/hooks-configuration | (no candidate files outside exclusion list teach the `allow`/`ask` decision shape — `labs/lab06.md` teaches the older exit-code semantics ("non-zero exit blocks"), which is correct and deny-only by construction) | n/a | n/a | n/a | drop — in-bounds prose does not assert the inaccurate `allow`/`ask`/`deny` triad; lab06's exit-code framing is consistent with current docs |
| `~/.copilot/config.json` is the Copilot CLI settings file | https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-config-dir-reference | (no candidate files outside exclusion list — only `docs/copilot-config-reference.md` matched, and it is WS-B sibling output) | n/a | n/a | n/a | drop — no in-bounds teaching of the legacy `config.json` filename |

## Summary

- **apply (2):** prompts-as-CLI claim → 4 files; MCP `sse` → 1 file. 5 file edits total, each a single-blockquote soft callout (≤ 2 sentences) per plan §184.
- **drop (5):** no in-bounds material to correct after the exclusion subtraction, OR claim is structural narrative that fails Q3.
- **Commit grouping (plan §195 — one commit per *class* of correction):** group A = "prompt files are IDE-only" (4 files); group B = "MCP `sse` deprecated in favor of `http`" (1 file). Working tree left dirty for orchestrator commit.
